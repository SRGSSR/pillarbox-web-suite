&:not(.vjs-audio-only-mode) .vjs-control-bar {
  background: linear-gradient(transparent, var(--{{kebabCase name}}-background));
}

&.vjs-audio-only-mode .vjs-control-bar {
  background: var(--{{kebabCase name}}-background);
}
